# V0.2 - 基础架构搭建

> **目标**: 完成服务拆分，引入登录鉴权和数据库，建立完整的微服务架构

## 完成时间
- 预计: 2-3 天

## 功能特性

### 核心功能
1. **LobbyServer** - 登录鉴权入口
2. **DBServer** - 数据持久化 (MySQL)
3. **SceneServer** - 场景服务 (改造自 V0.1)
4. **账号系统** - QQ号模拟登录
5. **角色系统** - 角色创建、查询

### 服务架构
```
┌─────────────────────────────────────────────────────────────────────────┐
│                                Client                                   │
│                     【单一接入点：只连接 LobbyServer】                    │
│                        Tars 协议 (TCP)                                   │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ ① 所有请求都通过 LobbyServer 转发
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          LobbyServer                                   │
│                    【统一接入层 / 网关层】                                │
│                                                                            │
│  - 客户端连接管理（connId ↔ playerId 映射）                               │
│  - 账号鉴权                                                              │
│  - 角色列表/创建/选择                                                     │
│  - 请求路由与转发                                                         │
│  - 推送回调管理                                                           │
└───────────┬─────────────────────────────────────────────────────────────┘
            │
            │ ② Tars RPC (内部调用，Client 不可见)
            │
            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      【内部服务 - Client 不可见】                         │
│                                                                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────┐  │
│  │    DBServer     │  │  SceneServer   │  │   其他服务 (V0.4+)       │  │
│  │    (20010)      │  │   (20002+)     │  │   BattleServer 等       │  │
│  │                 │  │                │  │                         │  │
│  │ - MySQL 账号/角色│  │ - 场景管理     │  │ - 战斗/匹配/社交        │  │
│  │ - Tcaplus 位置  │  │ - 移动处理     │  │ - 按需扩展              │  │
│  │ - playerId 分配 │  │ - 位置广播     │  │                         │  │
│  └─────────────────┘  │ - AOI (V0.4+) │  └─────────────────────────┘  │
│                       └─────────────────┘                               │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                              通信流程                                    │
├─────────────────────────────────────────────────────────────────────────┤
│  Client ──▶ LobbyServer ──▶ DBServer      (登录/账号/角色/playerId)      │
│  Client ──▶ LobbyServer ──▶ SceneServer   (进入场景/移动请求)            │
│                                                                          │
│  SceneServer ──▶ LobbyServer ──▶ Client  (回调推送：其他玩家移动等)       │
│                                                                          │
│  内部服务直接通信: SceneServer ──▶ DBServer (位置持久化)                  │
└─────────────────────────────────────────────────────────────────────────┘
```

## 技术方案

### 服务目录结构

```
server/
├── tars/                      # ⭐ 共享协议文件 (所有服务共用)
│   │
│   │   # ===== 公共基础协议 =====  #
│   ├── GameDemoBase.tars      # 公共结构体、枚举、错误码 (共用)
│   │   ├── enum ErrorCode
│   │   ├── PlayerInfo
│   │   ├── PlayerMoveNotify
│   │   ├── PlayerEnterNotify
│   │   ├── PlayerLeaveNotify
│   │   ├── SceneInfo
│   │   └── GameCallback        # 回调接口
│   │
│   │   # ===== 各服务专用协议 =====  #
│   ├── Account.tars           # 账号相关接口 (LobbyServer 使用)
│   │   ├── LoginReq/Resp
│   │   ├── RegisterReq/Resp
│   │   ├── RoleInfo
│   │   ├── CreateRoleReq/Resp
│   │   ├── GetRoleListReq/Resp
│   │   ├── SelectRoleReq/Resp
│   │   └── AccountServant      # 账号服务接口
│   │
│   ├── Scene.tars              # 场景相关接口 (SceneServer 使用)
│   │   ├── EnterSceneReq/Resp
│   │   ├── MoveReq/Resp
│   │   ├── HeartBeatReq/Resp
│   │   └── SceneServant        # 场景服务接口
│   │
│   ├── DB.tars                  # 数据库接口 (DBServer 使用)
│   │   ├── DBAccountServant    # 账号数据操作
│   │   └── DBRoleServant       # 角色数据操作
│   │
│   │   # ===== tars2cpp 生成的文件 =====  #
│   ├── GameDemoBase.h          # 包含所有生成的公共头文件
│   ├── GameDemoBase.cpp
│   ├── Account.h/cpp           # Account.tars 生成
│   ├── Scene.h/cpp              # Scene.tars 生成
│   └── DB.h/cpp                 # DB.tars 生成
│   │
│
│   ├── LobbyServer/             # LobbyServer 服务
│   │   ├── LobbyImp.h/cpp      # 服务实现
│   │   ├── LobbyServer.cpp     # 主入口
│   │   ├── ConnectionManager.h/cpp  # 连接管理
│   │   └── build/              # 编译输出
│   │
│   ├── SceneServer/             # SceneServer 服务
│   │   ├── SceneImp.h/cpp
│   │   ├── SceneServer.cpp
│   │   └── build/
│   │
│   └── DBServer/                # DBServer 服务
│       ├── DBImp.h/cpp
│       ├── DBServer.cpp
│       └── build/
```

> **重要说明**：
> 1. 所有 `.tars` 文件放在 `server/tars/` 目录
> 2. 执行一次 `tars2cpp *.tars` 生成所有头文件到 `server/tars/`
> 3. 各服务编译时通过 `makefile` 添加 `-I../tars` 包含公共头文件
> 4. 结构体在 `GameDemoBase.tars` 中定义一次，所有服务共用

### .tars 接口设计

```tars
// ========== server/tars/GameDemoBase.tars ==========
// 公共基础协议，所有服务共用

module GameDemo {

    // 通用错误码
    enum ErrorCode {
        SUCCESS = 0,
        ERR_ACCOUNT_NOT_FOUND = 1001,
        ERR_PASSWORD_WRONG = 1002,
        ERR_ROLE_NOT_FOUND = 2001,
        ERR_SCENE_NOT_FOUND = 3001,
        ERR_SERVER_BUSY = 9999,
    };

    // 玩家基础信息
    struct PlayerInfo {
        0 require long playerId;
        1 require string openId;
        2 require string roleName;
        3 require int level;
        4 require float x;
        5 require float y;
        6 require float z;
        7 require int sceneId;
    };

    // 场景信息
    struct SceneInfo {
        0 require int sceneId;
        1 require string sceneName;
        2 require int playerCount;
        3 require int maxPlayer;
    };

    // 移动通知
    struct PlayerMoveNotify {
        0 require long playerId;
        1 require float x;
        2 require float y;
        3 require float z;
        4 require int64 timestamp;
    };

    // 玩家进入通知
    struct PlayerEnterNotify {
        0 require PlayerInfo player;
        1 require int64 timestamp;
    };

    // 玩家离开通知
    struct PlayerLeaveNotify {
        0 require long playerId;
        1 require int64 timestamp;
    };

    // 回调接口 (推送用)
    interface GameCallback {
        oneway void onPlayerMove(1 require PlayerMoveNotify notify);
        oneway void onPlayerEnter(1 require PlayerEnterNotify notify);
        oneway void onPlayerLeave(1 require PlayerLeaveNotify notify);
        oneway void onHeartbeat(1 require int64 serverTime);
    };

};
```

```tars
// ========== server/tars/Account.tars ==========
// 账号相关接口

#include "GameDemoBase.tars"

module GameDemo {

    // 登录请求
    struct LoginReq {
        0 require string qqNumber;
        1 require string password;
    };
    
    struct LoginRsp {
        0 require int ret;
        1 optional string msg;
        2 optional long playerId;
        3 optional long accountId;
    };
    
    // 注册请求
    struct RegisterReq {
        0 require string qqNumber;
        1 require string password;
    };
    
    struct RegisterRsp {
        0 require int ret;
        1 optional string msg;
    };
    
    // 角色信息
    struct RoleInfo {
        0 require long roleId;
        1 require string roleName;
        2 require int level;
        3 require int sceneId;
    };
    
    // 创建角色
    struct CreateRoleReq {
        0 require long accountId;
        1 require string roleName;
    };
    
    struct CreateRoleRsp {
        0 require int ret;
        1 optional string msg;
        2 optional RoleInfo role;
    };
    
    // 获取角色列表
    struct GetRoleListReq {
        0 require long accountId;
    };
    
    struct GetRoleListRsp {
        0 require int ret;
        1 optional vector<RoleInfo> roles;
    };
    
    // 选择角色进入游戏
    struct SelectRoleReq {
        0 require long accountId;
        1 require long roleId;
    };
    
    struct SelectRoleRsp {
        0 require int ret;
        1 optional string msg;
        2 optional string sceneServerAddr;
        3 optional RoleInfo role;
    };
    
    interface AccountServant {
        // 登录
        int login(1 require LoginReq req, 2 require LoginRsp rsp);
        
        // 注册
        int registerAccount(1 require RegisterReq req, 2 require RegisterRsp rsp);
        
        // 创建角色
        int createRole(1 require CreateRoleReq req, 2 require CreateRoleRsp rsp);
        
        // 获取角色列表
        int getRoleList(1 require GetRoleListReq req, 2 require GetRoleListRsp rsp);
        
        // 选择角色
        int selectRole(1 require SelectRoleReq req, 2 require SelectRoleRsp rsp);
    };
};
```

```tars
// ========== server/tars/Scene.tars ==========
// 场景相关接口

#include "GameDemoBase.tars"

module GameDemo {

    // 进入场景
    struct EnterSceneReq {
        0 require long playerId;
        1 require int sceneId;
    };
    
    struct EnterSceneRsp {
        0 require int ret;
        1 optional string msg;
        2 optional PlayerInfo self;
        3 optional vector<PlayerInfo> players;
    };
    
    // 移动
    struct MoveReq {
        0 require long playerId;
        1 require float x;
        2 require float y;
        3 require float z;
    };
    
    struct MoveRsp {
        0 require int ret;
        1 optional string msg;
    };
    
    // 心跳
    struct HeartBeatReq {
        0 require long playerId;
    };
    
    interface SceneServant {
        int enterScene(1 require EnterSceneReq req, 2 require EnterSceneRsp rsp);
        int move(1 require MoveReq req, 2 require MoveRsp rsp);
        int heartbeat(1 require HeartBeatReq req);
    };
};
```

```tars
// ========== server/tars/DB.tars ==========
// 数据库相关接口

#include "GameDemoBase.tars"

module GameDemo {

    // 账号数据操作
    interface DBAccountServant {
        int createAccount(1 require string qqNumber, 2 require string password, 3 out long accountId);
        int getAccountByQQ(1 require string qqNumber, 2 out long accountId, 3 out string password);
        int accountExists(1 require string qqNumber, 2 out bool exists);
    };
    
    // 角色数据操作
    interface DBRoleServant {
        int createRole(1 require long accountId, 1 require string roleName, 2 out long playerId, 3 out RoleInfo role);
        int getRoleList(1 require long accountId, 2 out vector<RoleInfo> roles);
        int getRole(1 require long playerId, 2 out RoleInfo role);
        int updateRole(1 require RoleInfo role);
    };
};
```

### MySQL 表结构

```sql
-- 账号表
CREATE TABLE `t_account` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `qq_number` VARCHAR(20) UNIQUE NOT NULL,
    `password` VARCHAR(128) NOT NULL,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_qq_number (`qq_number`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 角色表
CREATE TABLE `t_role` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `account_id` BIGINT NOT NULL,
    `role_name` VARCHAR(64) NOT NULL,
    `level` INT DEFAULT 1,
    `scene_id` INT DEFAULT 1,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_account_id (`account_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 连接管理设计（架构 A：LobbyServer 统一接入）

> **核心原则**：Client 只连接 LobbyServer，所有请求和推送都经过 LobbyServer

#### 1. 连接架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端 (Client)                           │
│                 【单一接入：只连接 LobbyServer】                   │
└──────────┬──────────────────────────────────────────────────────┘
           │
           │ 1. 所有请求都通过 LobbyServer
           ▼
┌─────────────────────┐       ┌─────────────────────┐
│   LobbyServer       │       │   SceneServer       │
│   (20001)          │       │   (20002+)          │
│                    │       │                     │
│ - 连接接入管理       │       │ - 场景管理          │
│ - playerId 验证      │       │ - 移动处理          │
│ - 角色列表          │       │ - 位置广播          │
│ - 请求路由转发       │       │                     │
└──────────┬──────────┘       └──────────┬──────────┘
           │                              │
           │ Tars RPC (内部调用)           │ Tars RPC (内部调用)
           └──────────┬───────────────────┘
                      │
                      ▼
           ┌─────────────────────┐
           │     DBServer        │
           │     (20010)         │
           │ 【内部服务，不可见】  │
           └─────────────────────┘
```

#### 2. 连接生命周期（工业规范版）

> **设计原则**：不维护应用层 Session，直接利用 Tars 框架的连接管理能力
> - `connectionId`：Tars 原生连接标识，连接建立时自动分配，断开时自动失效
> - `playerId`：业务层玩家唯一标识，登录后由 DBServer 分配并返回

```cpp
客户端                           服务端 (LobbyServer)
   │                               │
   │────── 1. TCP连接 ─────────────▶│ Tars 分配 connectionId
   │◀───── 连接成功 (connId) ─────│ 记录: connId -> 空
   │                               │
   │────── 2. Login ──────────────▶│ 验证 openId，申请 playerId
   │◀───── playerId ──────────────│ 记录: connId -> playerId
   │                               │
   │────── 3. CreateRole ─────────▶│ DBServer 创建角色
   │◀───── roleInfo ──────────────│
   │                               │
   │────── 4. EnterScene ─────────▶│ 路由到目标 SceneServer
   │                               │ 记录: playerId ↔ sceneId
   │                               │
   │◀───── 5. 推送: 其他人移动 ──────│ SceneServer 推送
   │◀───── 6. 推送: 其他人进入 ──────│
   │                               │
   │────── 7. Move ───────────────▶│ 更新位置
   │                               │▶ SceneServer 广播
   │                               │
   │────── 8. 断线 ───────────────▶│ LobbyServer 检测到断连
   │                               │ 清理: connId -> playerId
   │                               │ 通知 SceneServer 玩家离线
```

**关键设计说明**：

| 设计点 | 传统方案 (Session) | 工业规范方案 (无 Session) |
|--------|-------------------|--------------------------|
| 连接标识 | sessionId (需生成/存储/过期) | connectionId (Tars 原生) |
| 身份验证 | 每次请求携带 sessionId 校验 | connId 隐式绑定 playerId |
| 内存管理 | 需定期清理过期 session | connId 断开即失效，无需维护 |
| 安全性 | session 可能被伪造 | Tars 底层保证连接可信 |
| 性能 | 多一次 map 查找 | 直接映射 |

**LobbyServer 连接管理模块（简化版）**：

```cpp
// ConnectionManager.h - 无 Session 版本
class ConnectionManager {
public:
    // 登录时绑定
    void bindPlayer(uint32_t connId, int64_t playerId, const string& openId);
    
    // 断线时解绑
    void unbindConnection(uint32_t connId);
    
    // 通过 connId 获取 playerId
    int64_t getPlayerId(uint32_t connId);
    
    // 通过 playerId 获取 connId
    uint32_t getConnectionId(int64_t playerId);
    
    // 心跳更新（可选，用于监控）
    void updateHeartbeat(uint32_t connId);
    
    // 踢出玩家
    void kickPlayer(int64_t playerId, const string& reason);

private:
    // 连接信息: connId -> PlayerSession (轻量结构)
    std::unordered_map<uint32_t, PlayerSession> _connections;
    
    // 反向索引: playerId -> connId
    std::unordered_map<int64_t, uint32_t> _playerToConn;
    
    // 定时清理过期连接（心跳超时）
    void cleanupExpiredConnections();
};

// 简化后的数据结构
struct PlayerSession {
    int64_t playerId;
    string openId;
    int64_t lastHeartbeat;
    // 不需要 sessionId，connectionId 就是天然标识
};
```

#### 3. 推送机制设计（架构 A：LobbyServer 统一推送）

> **核心设计**：Client 只连接 LobbyServer，所有推送经过 LobbyServer 转发
> - Client 长连接 LobbyServer，不断开
> - LobbyServer 持有所有在线玩家的 connId
> - SceneServer 处理业务后返回给 LobbyServer，LobbyServer 负责推送

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           连接架构 (架构 A)                             │
├─────────────────────────────────────────────────────────────────────────┤
│  LobbyServer (端口 20001)                                              │
│    - 长连接：Client 一直保持连接                                        │
│    - 管理：connId ↔ playerId 映射                                       │
│    - 职责：登录、大厅、路由转发、推送回调                                 │
│                                                                         │
│  SceneServer (端口 20002+)                                              │
│    - 无需维护 Client 连接                                                │
│    - 内部 RPC 服务                                                      │
│    - 处理场景业务，返回结果给 LobbyServer                                │
└─────────────────────────────────────────────────────────────────────────┘
```

**推送流程（完整路径）**：

```
玩家 A Move           LobbyServer          SceneServer          玩家 B
   │                      │                     │                   │
   │──── Move ────────────▶│──── Move ─────────▶│                   │
   │                      │                     │                   │
   │                      │◀─── Rsp + 广播 ─────│                   │
   │                      │                     │                   │
   │                      │──── onPlayerMove ──────────────────────▶│
   │◀─── Rsp ────────────│                     │                   │
```

**为什么 LobbyServer 统一推送**：

| 设计 | 推送路径 | 优点 | 缺点 |
|------|----------|------|------|
| **LobbyServer 统一推送（推荐）** | Scene → Lobby → 玩家 | 连接统一管理、安全性高 | 多一跳（<1ms 内网） |
| SceneServer 直连 | SceneServer → 玩家 | 路径最短 | 连接分散、无法统一管控 |

#### 3. 为什么不需要应用层 Session？

1. **Tars 框架天然具备连接管理能力**
   - 每个 TCP 连接分配唯一的 `connectionId`
   - 连接断开时 Tars 自动清理，无需业务层维护

2. **登录状态通过 connId → playerId 映射保持**
   - 登录时建立映射，客户端后续请求携带 playerId
   - 服务端验证 playerId 所属 connId 是否匹配当前连接

3. **安全性更强**
   - 传统 Session 可能被盗用伪造
   - connectionId 无法伪造，必须是真实 Tars 连接

4. **运维更简单**
   - 无需考虑 Session 过期策略
   - 无需 Session 同步（多节点部署）

#### 4. 连接参数配置

```cpp
// tars 配置文件
<Application>
    <Server>
        # 连接相关
        openGateway = 1              # 开启网关模式
        openNetThread = 1             # 开启网络线程
        netThreadNum = 4             # 网络线程数
        
        # 心跳相关
        heartbeatInterval = 30        # 心跳检测间隔(秒)
        connectionTimeout = 90        # 连接超时(秒)
        
        # 限流相关
        maxConnections = 10000        # 最大连接数
        minConnections = 10           # 最小连接数
    </Server>
</Application>
```

#### 5. 客户端连接策略（架构 A）

```cpp
class GameClient {
    uint32_t _connId;        // Tars 连接标识
    int64_t _playerId;       // 登录后获取
    std::string _openId;     // 本地保存用于重连
    GameCallbackPrx _callbackPrx;  // 回调代理
    
    // 登录成功后注册回调
    bool login(const string& openId) {
        // 1. 调用登录接口
        int ret = _lobbyPrx->login(openId, _playerId);
        if (ret != 0) return false;
        
        // 2. 创建回调对象并注册到 LobbyServer
        // ⚠️ 重要：登录成功后立即注册回调，否则无法接收推送
        _callbackPrx = new GameCallbackImp();  // 实现 GameCallback 接口
        _lobbyPrx->registerCallback(_playerId, _callbackPrx);
        
        // 3. 启动心跳保活
        startHeartbeat();
        
        return true;
    }
    
    // 心跳定时器
    void startHeartbeat() {
        _heartbeatTimer = std::thread([this]() {
            while (_running && _connected) {
                std::this_thread::sleep_for(std::chrono::seconds(30));
                if (_running && _connected) {
                    sendHeartbeat(_playerId);  // 通过 LobbyServer 连接发送
                }
            }
        });
    }
    
    // 断线重连 (指数退避)
    void reconnect() {
        int retry = 0;
        while (_running && !_connected) {
            int delay = std::min(60, 1 << retry);  // 1s, 2s, 4s... 最大60s
            std::this_thread::sleep_for(std::chrono::seconds(delay));
            
            // 重连到 LobbyServer
            if (tryConnectLobby()) {
                // 重新登录验证
                if (relogin(_openId)) {
                    // ⚠️ 重新注册回调
                    _callbackPrx = new GameCallbackImp();
                    _lobbyPrx->registerCallback(_playerId, _callbackPrx);
                    // 重连成功，恢复场景状态
                    restoreSceneState();
                }
            }
            retry++;
        }
    }
};
```

**重连流程说明**：
1. 客户端本地保存 `openId`
2. 断线后重连到 LobbyServer
3. 调用 `login(openId)` 重新获取 `playerId`
4. ⚠️ **重新注册回调**（重要！）
5. 调用 `reenterScene(playerId)` 恢复场景状态

#### 6. 服务端内部通信

| 通信路径 | 协议 | 用途 |
|----------|------|------|
| Client ↔ LobbyServer | Tars RPC (TCP) | 登录、大厅操作、场景请求、推送回调 |
| LobbyServer → SceneServer | Tars RPC | 转发场景请求 |
| SceneServer → LobbyServer | Tars RPC | 返回结果、离线通知 |
| LobbyServer ↔ DBServer | Tars RPC | 账号/角色数据读写 |
| SceneServer ↔ DBServer | Tars RPC | 位置数据读写 |

> **说明**：Client 只连接 LobbyServer，SceneServer 无需维护 Client 连接

---

### 服务端推送机制 (Server Push)

#### 问题：服务端如何主动推送消息给客户端？

Tars 本身是**请求-响应**模式，不支持服务端主动推送。有以下几种方案：

#### 方案对比

| 方案 | 优点 | 缺点 | 推荐场景 |
|------|------|------|----------|
| **Tars 双向连接** | 与 Tars 集成好 | 实现复杂 | Tars 生态 |
| **WebSocket** | 成熟、广泛支持 | 需要额外服务 | Web 客户端 |
| **Protobuf RPC** | 高效、跨语言 | 需要独立服务 | 高性能游戏 |
| **轮询** | 简单 | 延迟高、资源浪费 | 低频更新 |

#### 推荐：Tars 双向连接 + 回调模式

> **重要**：根据 Tars 官方文档，推送回调接口**必须声明为 `oneway`**，否则服务端会等待客户端响应导致阻塞！

```tars
// Scene.tars - 双向通信接口

module GameDemo {

    // 玩家移动通知 (服务端→客户端)
    struct PlayerMoveNotify {
        0 require int64 playerId;
        1 require float x;
        2 require float y;
        3 require float z;
        4 require int64 timestamp;
    };
    
    // 玩家进入通知
    struct PlayerEnterNotify {
        0 require PlayerInfo player;
        1 require int64 timestamp;
    };
    
    // 玩家离开通知
    struct PlayerLeaveNotify {
        0 require int64 playerId;
        1 require int64 timestamp;
    };

    // 客户端回调接口 (由客户端实现，服务端调用)
    // ⚠️ 重要：必须使用 oneway，否则服务端会阻塞等待响应
    interface GameCallback {
        // 移动同步回调
        oneway void onPlayerMove(1 require PlayerMoveNotify notify);
        
        // 玩家进入回调
        oneway void onPlayerEnter(1 require PlayerEnterNotify notify);
        
        // 玩家离开回调
        oneway void onPlayerLeave(1 require PlayerLeaveNotify notify);
        
        // 心跳响应
        oneway void onHeartbeat(1 require int64 serverTime);
    };
    
    // Lobby 服务接口 (客户端调用)
    interface LobbyServant {
        // 登录成功后注册回调
        int registerCallback(1 require int64 playerId, 2 require GameCallback* callback);
        
        // 心跳
        int heartbeat(1 require int64 playerId);
    };
    
    // 场景服务主接口
    interface SceneServant {
        // 进入场景时传递回调
        int enterScene(1 require int64 playerId, 
                       2 require GameCallback* callback,
                       3 require PlayerInfo& self,
                       4 require vector<PlayerInfo> players);
        
        // 移动
        int move(1 require int64 playerId, 
                 2 require float x, 
                 3 require float y,
                 4 require float z);
        
        // 心跳
        int heartbeat(1 require int64 playerId);
    };
};
```

> **Tars Push Callback 机制说明**：
> 1. 回调接口必须使用 `oneway void`，否则服务端调用时会阻塞等待客户端响应
> 2. 客户端在登录成功后应立即调用 `registerCallback` 注册回调
> 3. 客户端需要定期发送心跳（调用任意接口）保持连接存活
> 4. LobbyServer 保存 `playerId -> GameCallbackPrx` 映射用于推送

#### 服务端推送实现（架构 A）

```cpp
// SceneImp.cpp - SceneServer 只处理业务，返回结果给 LobbyServer
class SceneImp : public SceneServant {
private:
    // 场景内玩家: playerId -> PlayerInfo
    std::unordered_map<int64_t, PlayerInfo> _scenePlayers;
    
public:
    // 玩家移动
    tars::Int32 move(tars::Int64 playerId, tars::Float x, tars::Float y, tars::Float z) {
        // 1. 更新位置
        auto& player = _scenePlayers[playerId];
        player.x = x;
        player.y = y;
        player.z = z;
        
        // 2. 构造通知（包含需要推送的玩家列表）
        PlayerMoveNotify notify;
        notify.playerId = playerId;
        notify.x = x;
        notify.y = y;
        notify.z = z;
        notify.timestamp = TC_Common::now2ms();
        
        // 3. 收集场景内其他玩家的 playerId
        std::vector<int64_t> otherPlayerIds;
        for (auto& kv : _scenePlayers) {
            if (kv.first != playerId) {
                otherPlayerIds.push_back(kv.first);
            }
        }
        
        // 4. 返回给 LobbyServer，由 LobbyServer 负责推送
        return makeMoveRsp(notify, otherPlayerIds);
    }
};

// LobbyImp.cpp - LobbyServer 负责推送
class LobbyImp : public LobbyServant {
private:
    // connId ↔ playerId 映射
    std::unordered_map<uint32_t, PlayerSession> _connections;
    std::unordered_map<int64_t, uint32_t> _playerToConn;
    
    // playerId -> CallbackPrx (Client 回调)
    std::unordered_map<int64_t, GameCallbackPrx> _callbacks;
    
public:
    // 收到 SceneServer 的推送请求
    void onPlayerMove(int64_t playerId, const PlayerMoveNotify& notify,
                      const vector<int64_t>& otherPlayerIds) {
        // 遍历其他玩家，通过 LobbyServer 推送
        for (int64_t otherId : otherPlayerIds) {
            auto it = _callbacks.find(otherId);
            if (it != _callbacks.end()) {
                // 异步推送，不阻塞主流程
                it->second->async_onPlayerMove(notify);
            }
        }
    }
};
```

#### 客户端接收实现

```cpp
// 客户端回调实现类
class GameCallbackImp : public GameCallback {
public:
    void onPlayerMove(const PlayerMoveNotify& notify) override {
        // 更新本地其他玩家位置
        updateOtherPlayerPos(notify.playerId, notify.x, notify.y, notify.z);
        cout << "[Sync] Player " << notify.playerId 
             << " moved to (" << notify.x << ", " << notify.y << ")" << endl;
    }
    
    void onPlayerEnter(const PlayerEnterNotify& notify) override {
        // 添加新玩家
        addPlayer(notify.player);
        cout << "[Sync] Player " << notify.player.roleName << " entered" << endl;
    }
    
    void onPlayerLeave(const PlayerLeaveNotify& notify) override {
        // 移除玩家
        removePlayer(notify.playerId);
        cout << "[Sync] Player " << notify.playerId << " left" << endl;
    }
};
```

#### 推送时序图（架构 A：LobbyServer 统一推送）

```
玩家 A                    LobbyServer          SceneServer          玩家 B
   │                          │                     │                   │
   │──── 1. Login ────────────▶│                     │                   │
   │◀── playerId ─────────────│                     │                   │
   │                          │                     │                   │
   │──── 2. registerCallback ──▶│  保存 playerId→CallbackPrx            │
   │◀── 成功 ─────────────────│                     │                   │
   │                          │                     │                   │
   │──── 3. Move ─────────────▶│──── Move ─────────▶│                   │
   │                          │                     │                   │
   │                          │◀─── Rsp ───────────│                   │
   │                          │                     │                   │
   │                          │──── onPlayerMove ──────────────────────▶│
   │◀─── Move Rsp ───────────│                     │                   │
```

**说明**：
1. 玩家登录成功后，LobbyServer 返回 `playerId`，**不断开连接**
2. 客户端一直保持与 LobbyServer 的连接
3. Move 请求发送到 LobbyServer，LobbyServer 转发给 SceneServer
4. SceneServer 处理后返回结果，LobbyServer 查找其他玩家的 connId 并推送

---

### 断线处理（架构 A：LobbyServer 统一管理）

```cpp
// LobbyServer 端：连接断开时自动调用
void LobbyImp::onConnectionClose(uint32_t connId) {
    auto it = _connections.find(connId);
    if (it == _connections.end()) return;
    
    PlayerSession session = it->second;
    
    // 1. 从连接映射移除
    _playerToConn.erase(session.playerId);
    _connections.erase(connId);
    
    // 2. 通知 SceneServer 玩家离线
    sceneProxy->onPlayerOffline(session.playerId, session.sceneId);
    
    // 3. 广播玩家离开通知给其他在线玩家
    PlayerLeaveNotify notify;
    notify.playerId = session.playerId;
    notify.timestamp = TC_Common::now2ms();
    broadcastToOnlinePlayers(notify);  // LobbyServer 查找其他玩家的 connId 并推送
}
```

> **注意**：LobbyServer 检测到断连后，SceneServer 也会收到通知，清理场景内的玩家数据

## 实现步骤

### Step 1: 准备协议文件
```bash
cd /workspace/server/tars

# 创建并编辑协议文件
vim GameDemoBase.tars    # 公共结构体
vim Account.tars         # 账号接口
vim Scene.tars           # 场景接口
vim DB.tars              # 数据库接口

# 生成头文件 (生成到当前目录)
tars2cpp *.tars
```

### Step 2: 创建 DBServer
```bash
cd /workspace/server
/usr/local/tars/cpp/script/cmake_tars_server.sh DBServer DBServer DB

# 在 build 目录编译
cd DBServer/build
make -j4
```

### Step 3: 实现 DBServer
- 连接 MySQL (使用 mysqlclient)
- 实现账号 CRUD
- 实现角色 CRUD（分配 playerId）
- 编译: `cd ../DBServer/build && make`

### Step 4: 创建 LobbyServer
```bash
cd /workspace/server
/usr/local/tars/cpp/script/cmake_tars_server.sh LobbyServer LobbyServer Lobby
```

### Step 5: 实现 LobbyServer
- 调用 DBServer 进行账号操作
- **连接管理**（connId ↔ playerId 映射，无 Session）
- 登录/注册/创角/选角逻辑
- 转发场景请求到 SceneServer
- 编译: `cd ../LobbyServer/build && make`

> **LobbyServer 引用公共头文件**：
> ```makefile
> # LobbyServer/build/Makefile 或 CMakeLists.txt
> INCLUDES += -I../tars
> ```

### Step 6: 创建 SceneServer
```bash
cd /workspace/server
/usr/local/tars/cpp/script/cmake_tars_server.sh SceneServer SceneServer Scene
```

### Step 7: 实现 SceneServer
- 接收 LobbyServer 转发的请求
- 玩家位置管理
- 返回结果给 LobbyServer（LobbyServer 负责推送）
- 编译: `cd ../SceneServer/build && make`

> **SceneServer 引用公共头文件**：
> ```makefile
> # SceneServer/build/Makefile 或 CMakeLists.txt
> INCLUDES += -I../tars
> ```

### Step 8: 改造客户端
- 登录界面
- 注册界面
- 角色创建界面
- 角色列表界面
- 进入场景

### Step 9: 数据库初始化
- 编写 init_db.sql
- 执行初始化脚本

## 验收标准

- [ ] 客户端能完成注册流程
- [ ] 客户端能完成登录流程
- [ ] 客户端能创建角色
- [ ] 客户端能查看角色列表
- [ ] 客户端能选择角色进入场景
- [ ] 场景内移动同步功能正常
- [ ] 数据正确持久化到 MySQL

## 潜在风险

| 风险 | 应对措施 |
|------|----------|
| MySQL 连接池配置 | 根据玩家数量调整连接池大小 |
| 服务间调用延迟 | 使用 Tars 异步调用优化 |
| **海量连接内存** | 使用 `ConnectionManager` 合理管理连接生命周期（connId 自动失效） |
| **断线未清理** | Tars 框架自动检测，`onConnectionClose` 回调清理业务数据 |
| **playerId 冲突** | DBServer 使用自增/UUID 保证唯一性 |

## 下一步

V0.3 将引入：
- TcaplusDB 位置存储
- 多场景支持
- 角色状态同步
