# V0.4 - 移动同步增强

> **目标**: 优化移动同步性能，实现 AOI 九宫格算法

## 完成时间
- 预计: 3-4 天

## 功能特性

### 核心功能
1. **AOI 九宫格** - 视野管理，减少无效广播
2. **移动节流** - 限制移动请求频率
3. **批量推送** - 合并推送减少网络开销
4. **断线重连** - 完善的断线重连机制

### 性能目标
| 指标 | 目标值 |
|------|--------|
| 单服同时在线 | 500+ |
| 移动同步延迟 | < 50ms |
| 广播消息量 | 减少 70% |

## 技术方案

### AOI 九宫格算法

```
    ┌───────┬───────┬───────┐
    │   1   │   2   │   3   │     场景被划分为网格
    │  M-1  │  M,M  │  M+1  │     每个格子为固定大小
    ├───────┼───────┼───────┤     玩家视野 = 3x3 或 5x5 九宫格
    │   4   │   5   │   6   │     
    │ M-1,N │ M,N   │ M+1,N │     移动时只通知视野内玩家
    ├───────┼───────┼───────┤     进出视野时才广播
    │   7   │   8   │   9   │     格子内移动不广播
    │M-1,N+1│M,N+1  │M+1,N+1│
    └───────┴───────┴───────┘
```

### 配置参数
```tars
// 配置文件
AOI_GRID_SIZE = 10.0       // 格子大小(米)
AOI_VIEW_RANGE = 30.0      // 视野范围(米)
MOVE_FREQUENCY_LIMIT = 10  // 每秒最多移动10次
BROADCAST_INTERVAL = 50    // 批量广播间隔(ms)
```

### .tars 接口增强

```tars
// Scene.tars 增强
module GameDemo {

    // 增强版移动请求
    struct MoveReqEx {
        0 require long playerId;
        1 require float x;
        2 require float y;
        3 require float z;
        4 require int direction;      // 移动方向
        5 require long clientTime;    // 客户端时间戳
    };
    
    // 批量玩家信息
    struct PlayerBatchNotify {
        0 require vector<PlayerInfo> enterPlayers;   // 进入视野
        1 require vector<long> leavePlayerIds;       // 离开视野
        2 require vector<PlayerInfo> movePlayers;    // 移动更新
    };
    
    interface SceneServant {
        // 原有接口...
        int move(1 require MoveReq req, 2 require MoveRsp rsp);
        
        // 增强版移动
        int moveEx(1 require MoveReqEx req, 2 require MoveRsp rsp);
        
        // 获取视野内玩家
        int getViewPlayers(1 require long playerId, 2 require GetViewPlayersRsp rsp);
    };
    
    // 客户端心跳
    interface SceneClient {
        // 原有接口...
        int onPlayerEnter(1 require PlayerInfo player);
        int onPlayerMove(1 require PlayerInfo player);
        int onPlayerLeave(1 require long playerId);
        
        // 批量推送
        int onBatchNotify(1 require PlayerBatchNotify notify);
    };
};
```

## 实现步骤

### Step 1: AOI 模块设计
```cpp
// AOIManager.h
class AOIManager {
public:
    // 初始化场景网格
    void init(int sceneWidth, int sceneHeight, float gridSize);
    
    // 进入场景
    void enter(Player* player);
    
    // 离开场景
    void leave(Player* player);
    
    // 移动
    void move(Player* player, float newX, float newY);
    
    // 获取视野内玩家
    vector<Player*> getViewPlayers(Player* player);
    
    // 获取改变的玩家(进入/离开)
    pair<vector<Player*>, vector<long>> getChangedPlayers(Player* player);
    
private:
    // 坐标转格子
    pair<int, int> posToGrid(float x, float y);
    
    // 格子坐标转格子ID
    int gridToId(int gx, int gy);
    
    // 九宫格邻居
    vector<int> getNeighborGrids(int gx, int gy, int range = 1);
    
private:
    float _gridSize;
    int _gridXCount;
    int _gridYCount;
    
    // 格子ID -> 玩家列表
    unordered_map<int, unordered_set<long>> _grids;
    
    // 玩家ID -> 玩家对象
    unordered_map<long, Player*> _players;
};
```

### Step 2: SceneServer 改造
- 集成 AOIManager
- 移动接口改造
- 批量推送定时器

### Step 3: 客户端改造
- 移动节流逻辑
- 批量消息处理
- 断线重连处理

### Step 4: 性能测试
- 使用 Bot 模拟 100 人移动
- 测量广播消息量
- 测量延迟

## 验收标准

- [ ] AOI 九宫格正确实现
- [ ] 只广播视野内玩家
- [ ] 进出视野正确通知
- [ ] 格子内移动不触发广播
- [ ] 500 人同屏性能达标
- [ ] 断线重连数据正确

## 潜在风险

| 风险 | 应对措施 |
|------|----------|
| 边界玩家处理 | 特殊格子标记 |
| 移动速度过快 | 客户端校验 + 服务端校验 |
| 内存占用 | 定期清理离线玩家 |

## 下一步

V0.5 将引入：
- 压测机器人
- 完整性能压测
- 性能指标验证
